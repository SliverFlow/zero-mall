syntax = "v1"

info(
    title: "user api file"
    desc: "用户相关 api "
    author: "SliverFlow"
    email: "202606540@qq.com"
    version: "v1.0"
)

type (
    Nil {
    }
)

// 手机验证码入参
type PhoneCaptcheReq {
    Phone string `json:"phone"`
}

// 用户端 用户信息出参
type UserReply {
}

// 用户端更新信息入参
type UserUpdateReq {
}

type RegisterByPhoneReq {
    Phone string `json:"phone"`
    Captche string `json:"captche"`
}
