syntax = "v1"

info(
	title: "order api file"
	desc: "订单相关 api"
	author: "SliverFlow"
	email: "202606540@qq.com"
	version: "v1.0"
)

import "api/order.api"

// user 需要鉴权的
@server(
	group : private
	prefix : v1
	middleware : Auth
)
service order {
	@handler orderFind
	post /user/find (Nil) returns (Nil);
}